<IfModule mod_rewrite.c>
    RewriteEngine On
    RewriteBase /sistemasilvina/

    # No redirigir si ya apunta a public/
    RewriteCond %{REQUEST_URI} !^/sistemasilvina/public

    # Redirigir todo lo demás a public/
    RewriteRule ^(.*)$ public/$1 [L]
</IfModule>
